# Additional dependencies for parallel PDF processing

# Rate limiting for API calls
rateguard>=1.0.0

# PDF to image conversion for vision models
pdf2image>=1.17.0

# System dependencies required (install separately):
# Ubuntu/Debian: sudo apt-get install poppler-utils
# macOS: brew install poppler
# Windows: Download poppler binaries from https://github.com/oschwartz10612/poppler-windows
